import CreditCard from "@/common/components/creditCard/CreditCard";
import "@/modules/loan/style.scss";

const Loan = () => {
    return (
        <main className="main">
            <CreditCard />
        </main>
    )
};

export default Loan;