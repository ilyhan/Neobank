import NotFound from "@/common/components/notFound/NotFound";

const NotFoundPage = () => {
    return (
        <main className="main">
            <NotFound/>
        </main>
    )
};

export default NotFoundPage;