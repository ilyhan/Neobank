export interface ICard {
    src: string;
    alt: string;
}

export interface ICreditCardDetails {
    title: string;
    description: string;
    tooltip: string;
}

export interface IStepGetCard {
    description: string;
}